datasource db {
  provider = "mysql"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model Patient {
  id        String   @id
  name      String
  room      String
  deviceId  String   @unique
  illness   String?
  condition String?
  createdAt DateTime @default(now())
}
